---
const pixelIds = (import.meta.env.META_PIXEL_ID ?? "")
  .split(",")
  .map((id: string) => id.trim())
  .filter(Boolean) as string[];
---

<script is:inline data-pixels={JSON.stringify(pixelIds)}>
  (function (f, b, e, v, n, t, s) {
    if (f.fbq) return;
    n = f.fbq = function () {
      n.callMethod ? n.callMethod.apply(n, arguments) : n.queue.push(arguments);
    };
    if (!f._fbq) f._fbq = n;
    n.push = n;
    n.loaded = !0;
    n.version = "2.0";
    n.queue = [];
    t = b.createElement(e);
    t.async = !0;
    t.src = v;
    s = b.getElementsByTagName(e)[0];
    s.parentNode.insertBefore(t, s);
  })(
    window,
    document,
    "script",
    "https://connect.facebook.net/en_US/fbevents.js"
  );

  var pixelsAttr =
    (document.currentScript &&
      document.currentScript.getAttribute("data-pixels")) ||
    "[]";
  var pixelIds = [];
  try {
    pixelIds = JSON.parse(pixelsAttr) || [];
  } catch (e) {
    pixelIds = [];
  }

  function getCookie(name) {
    var match = document.cookie.match(new RegExp("(^|; )" + name + "=([^;]*)"));
    return match ? decodeURIComponent(match[2]) : null;
  }

  pixelIds.forEach(function (id) {
    fbq("init", id, { fbc: getCookie("_fbc"), fbp: getCookie("_fbp") });
  });

  var getEventId = function () {
    var row = (document.cookie || "").split("; ").find(function (r) {
      return r.indexOf("event_id=") === 0;
    });
    return row ? row.split("=")[1] : undefined;
  };

  window.fbqTrack = function (event, data) {
    if (data === void 0) {
      data = {};
    }
    var eventId = getEventId();
    fbq("track", event, data, { eventID: eventId });
  };
  window.fbqTrack("PageView");
</script>
<noscript>
  {
    pixelIds.map((id: string) => (
      <img
        src={`https://www.facebook.com/tr?id=${id}&ev=PageView&noscript=1`}
        height="1"
        width="1"
        style="display:none"
      />
    ))
  }
</noscript>
